<nav class="navbar navbar-expand navbar-light bg-light sticky-top shadow-sm">
  <div class="container-fluid">
    <%= link_to "RoastBot", root_path, class: "navbar-brand fw-bold display-6" %>
    <div class="ms-auto">
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Sign Out", destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-outline-danger me-2" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Sign In", new_user_session_path, class: "btn btn-outline-secondary me-2" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary me-2" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
